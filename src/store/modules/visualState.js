const state = () => ({
  showMobileMenu: false
});

const getters = {
  getShowMobileMenu(state) {
    return state.showMobileMenu;
  }
};

const mutations = {};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions
};
